# ABI exports

`Quorum.json` and `CommitRevealVote.json` contain the JSON ABI arrays produced from the Solidity 0.8.26 build. Regenerate after interface changes with `forge inspect <Contract> abi` and commit the updated output.
